import javax.swing.JOptionPane;

public class CordellRecursion {

	public static void main(String[] args) {
		
   int i=Integer.parseInt(JOptionPane.showInputDialog("Enter a number"));
   int dcount = 0;
   String word=JOptionPane.showInputDialog("Enter a word"); //enter in a word
    // www.stackexchange.com splits word into different characters and puts each character into a separate array index where the array name is wordarr
	 
	mathrecursion.dcountmethod(i, dcount);
	StringRecursion.wordrecursion(word);
	MyRecursion.usernameMethod();
	}
}
